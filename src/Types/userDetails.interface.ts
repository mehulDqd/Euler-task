export interface UserDetails {
  address: string;
  appNetworkId: number;
  balance: string;
  daiBalance?: string;
  ethBalance?: number;
  mobileDevice: boolean;
  network: number;
  wallet: object;
}
